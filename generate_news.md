# 🚀 Автогенератор SEO-постов для DLE CMS 

Скрипт автоматически создаёт качественные SEO-оптимизированные новости/посты в DataLife Engine по заданной категории.

Идеально подходит для быстрого наполнения сайта услугами мастеров в Вильнюсе и Литве.

### ✨ Возможности
- Генерирует уникальные заголовки, short_story, full_story, keywords, metatitle
- Добавляет изображение в short_story
- Заполняет дополнительные поля (xfields → gorod)
- Защита паролем (чтобы никто не запустил случайно)
- Использует Prepared Statements (защита от SQL-инъекций)
- Полная отладка + понятные сообщения
- Работает с любым ID категории

### 📋 Требования
- DataLife Engine 10+ / 14+ (проверено на 14.x)
- PHP 7.4+
- MySQL / MariaDB
- Права на запись в таблицу `_post`

### 🛠 Установка

1. Скачай репозиторий
2. Загрузи `create-post.php` в корень сайта (или в папку `/engine/`)
3. Открой файл и укажи свои данные БД (строки 15-20)
4. (Рекомендуется) Создай файл `.htaccess` с защитой:

```apache
<Files "create-post.php">
    Order Deny,Allow
    Deny from all
    Allow from 127.0.0.1
    Allow from ::1
    # Добавь свой IP если запускаешь с другого компьютера
    # Allow from ТВОЙ_IP
</Files>
